pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/mcfemy/jenkins-demo-app.git'
            }
        }
        stage('Test') {
            steps {
                sh 'npm test'
            }
        }
    }
}
